<Window x:Class="LumenPomodoro.Views.DynamicIslandNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DynamicIslandNotification"
        SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Topmost="True"
        ShowInTaskbar="False" ShowActivated="False"
        Background="Transparent">
    <Border x:Name="PillBorder" CornerRadius="20" Background="#E01A1A1A"
            Padding="24,10" Margin="0"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" />
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="16" Opacity="0.4" Color="Black"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock x:Name="TitleBlock" FontSize="14" FontWeight="SemiBold"
                       Foreground="White" VerticalAlignment="Center"/>
            <TextBlock x:Name="CountdownBlock" FontSize="24" FontWeight="Light"
                       Foreground="White" VerticalAlignment="Center"
                       Margin="12,0,0,0" FontFamily="Inter Light"
                       Visibility="Collapsed"/>
            <TextBlock x:Name="MessageBlock" FontSize="12" Foreground="#CCFFFFFF"
                       Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>

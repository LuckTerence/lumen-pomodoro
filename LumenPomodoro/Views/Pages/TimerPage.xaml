<Page x:Class="LumenPomodoro.Views.Pages.TimerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:converters="clr-namespace:LumenPomodoro.Converters"
      xmlns:controls="clr-namespace:LumenPomodoro.Controls"
      ScrollViewer.CanContentScroll="False">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Themes/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:StatusToVisibilityConverter x:Key="StatusToVisibilityConverter" />
            <converters:StatusConverter x:Key="StatusConverter" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="32,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 任务选择 Chip -->
        <Border Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,24"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="12" Padding="12,6" Cursor="Hand"
                MouseDown="TaskName_MouseDown"
                Visibility="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Idle}">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="{Binding SelectedTask.Color, FallbackValue={StaticResource FallbackTaskColor}}" />
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center">
                    <Run Text="{Binding SelectedTask.Name, Mode=OneWay, FallbackValue='选择任务'}" />
                    <Run Text="  ▾" FontSize="10" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 状态标签 -->
        <TextBlock x:Name="StateLabelBlock" Grid.Row="2"
                   Text="{Binding CurrentStatus, Converter={StaticResource StatusConverter}}"
                   Margin="0,0,0,12">
            <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource StateLabel}">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Idle}" Value="Visible">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- 圆环进度 + 计时器数字 -->
        <Grid Grid.Row="3" HorizontalAlignment="Center" Margin="0,0,0,24" Width="260" Height="260">
            <controls:ArcProgress x:Name="ProgressRing" Progress="{Binding Progress}" StrokeThickness="6">
                <controls:ArcProgress.ForegroundArcBrush>
                    <SolidColorBrush Color="{Binding SelectedTask.Color, FallbackValue=#0078D4}" />
                </controls:ArcProgress.ForegroundArcBrush>
                <controls:ArcProgress.BackgroundArcBrush>
                    <SolidColorBrush Color="{StaticResource ArcBackgroundColor}" />
                </controls:ArcProgress.BackgroundArcBrush>
            </controls:ArcProgress>
            <TextBlock x:Name="TimerTextBlock" Text="{Binding RemainingTime}"
                       Style="{StaticResource TimerText}" FontSize="80"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </TextBlock.RenderTransform>
            </TextBlock>
        </Grid>

        <!-- 摄像头提醒 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16"
                    Visibility="{Binding IsCameraAlertActive, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Ellipse x:Name="CameraAlertDot" Width="8" Height="8" Fill="{DynamicResource AccentFillColorDefaultBrush}" Margin="0,0,8,0" />
            <TextBlock Text="摄像头提醒中" Style="{StaticResource Caption}" VerticalAlignment="Center" />
            <ui:Button Appearance="Secondary" Click="StopCameraButton_Click" FontSize="12" Padding="8,4" Margin="8,0,0,0">
                关闭
            </ui:Button>
        </StackPanel>

        <!-- 操作按钮区 -->
        <Grid Grid.Row="5">
            <StackPanel x:Name="IdlePanel" Orientation="Vertical" HorizontalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Idle}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFocusCompleted}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <ui:Button Appearance="Secondary" Click="AdjustTimeDown_Click" FontSize="14" Padding="10,4">-</ui:Button>
                    <TextBlock FontSize="15" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center" Margin="12,0">
                        <Run Text="{Binding AppSettings.WorkMinutes, Mode=OneWay}" FontWeight="SemiBold" />
                        <Run Text=" 分钟" />
                    </TextBlock>
                    <ui:Button Appearance="Secondary" Click="AdjustTimeUp_Click" FontSize="14" Padding="10,4">+</ui:Button>
                </StackPanel>
                <ui:Button Appearance="Primary" Click="StartFocusButton_Click" FontSize="17" Padding="24,12">
                    开始专注
                </ui:Button>
            </StackPanel>

            <StackPanel x:Name="FocusPanel"
                        Visibility="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Focus}"
                        Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:Button Appearance="Secondary" Click="PauseButton_Click" FontSize="14" Padding="16,8">暂停</ui:Button>
                <ui:Button Appearance="Secondary" Click="ResetButton_Click" FontSize="14" Padding="16,8" Margin="12,0,0,0">重置</ui:Button>
            </StackPanel>

            <StackPanel x:Name="PausedPanel"
                        Visibility="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Paused}"
                        Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:Button Appearance="Primary" Click="ResumeButton_Click" FontSize="17" Padding="24,12">继续</ui:Button>
                <ui:Button Appearance="Secondary" Click="ResetButton_Click" FontSize="14" Padding="16,8" Margin="12,0,0,0">重置</ui:Button>
            </StackPanel>

            <StackPanel x:Name="BreakPanel"
                        Visibility="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Break}"
                        Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:Button Appearance="Primary" Click="EndBreakButton_Click" FontSize="17" Padding="24,12">结束休息</ui:Button>
            </StackPanel>

            <StackPanel x:Name="CompletedPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsFocusCompleted}" Value="True" />
                                    <Condition Binding="{Binding CurrentStatus, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter=Idle}" Value="Visible" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <ui:Button Appearance="Primary" Click="StartShortBreakButton_Click" FontSize="17" Padding="24,12">短休息</ui:Button>
                <ui:Button Appearance="Secondary" Click="StartLongBreakButton_Click" FontSize="14" Padding="16,8" Margin="12,0,0,0">长休息</ui:Button>
                <ui:Button Appearance="Transparent" Click="SkipBreakButton_Click" FontSize="12" Padding="10,6" Margin="12,0,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}">跳过</ui:Button>
            </StackPanel>
        </Grid>

        <!-- 底部统计摘要 + 快捷键提示 -->
        <StackPanel Grid.Row="6" HorizontalAlignment="Center" Margin="0,20,0,0">
            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       HorizontalAlignment="Center" Cursor="Hand" MouseDown="StatsSummary_MouseDown">
                <Run Text="今日 " />
                <Run Text="{Binding TodayStats.CompletedPomodoros}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="SemiBold" />
                <Run Text=" · 专注 " />
                <Run Text="{Binding TodayStats.TotalFocusMinutes}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="SemiBold" />
                <Run Text=" 分" />
            </TextBlock>
            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       HorizontalAlignment="Center" Margin="0,8,0,0">
                <Run Text="空格键 开始/暂停 · Esc 重置" />
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>

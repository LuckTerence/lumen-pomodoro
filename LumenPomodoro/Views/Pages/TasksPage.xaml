<Page x:Class="LumenPomodoro.Views.Pages.TasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Themes/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="任务管理" Style="{StaticResource PageTitle}" Margin="0,0,0,16" />

        <Border Grid.Row="1" Margin="0,0,0,16"
                Background="{DynamicResource SpotifySurface1Brush}"
                CornerRadius="8" Padding="12">
            <StackPanel>
                <ui:TextBox x:Name="NewTaskNameBox" PlaceholderText="任务名称" Margin="0,0,0,10" />
                <StackPanel Orientation="Horizontal">
                    <ComboBox x:Name="ColorComboBox" Width="120" Margin="0,0,8,0" SelectedIndex="0" />
                    <Button Style="{StaticResource SpotifyButton}" Click="AddTask_Click" Padding="16,6">添加</Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      x:Name="TasksScrollViewer">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Tasks.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Tasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="14,12"
                                Background="{DynamicResource SpotifySurface2Brush}"
                                BorderBrush="{DynamicResource SpotifyDividerBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Ellipse Grid.Column="0" Width="10" Height="10" Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="{Binding Color}" />
                                    </Ellipse.Fill>
                                </Ellipse>

                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="15" FontWeight="Medium"
                                           Foreground="{DynamicResource SpotifyTextPrimaryBrush}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Style="{StaticResource SpotifyButton}" Click="SelectTask_Click" Tag="{Binding Id}" FontSize="12" Padding="8,4">
                                        选择
                                    </Button>
                                    <Button Style="{StaticResource SpotifyButton}" Click="EditTask_Click" Tag="{Binding Id}" FontSize="12" Padding="8,4" Margin="4,0,0,0">
                                        编辑
                                    </Button>
                                    <Button Style="{StaticResource SpotifyButton}" Click="DeleteTask_Click" Tag="{Binding Id}" FontSize="12" Padding="8,4" Margin="4,0,0,0">
                                        删除
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Tasks.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <ui:SymbolIcon Symbol="Add24" FontSize="36"
                           HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,12" />
            <TextBlock Text="还没有任务" FontSize="15" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="在下方添加你的第一个任务" Style="{StaticResource FinePrint}"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</Page>

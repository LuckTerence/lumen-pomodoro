<Window x:Class="LumenPomodoro.Views.TaskManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LumenPomodoro.Models"
        mc:Ignorable="d"
        Title="任务管理" Height="480" Width="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource GlassBackgroundBrush}" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" Color="{DynamicResource PrimaryTextColor}" Opacity="0.15" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource WindowBackgroundBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ErrorBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Border Style="{StaticResource GlassPanel}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource SecondaryButton}" Width="32" Height="32" Padding="0" Click="CloseButton_Click">
                        <TextBlock Text="&#xE92B;" FontFamily="Segoe MDL2 Assets" FontSize="14" />
                    </Button>
                </StackPanel>
                
                <TextBlock Grid.Row="1" Text="任务管理" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,16,0,0" />
                
                <ListBox Grid.Row="2" x:Name="TaskListBox" Margin="0,16,0,0" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                <Rectangle Width="12" Height="12" Fill="{Binding Color}" RadiusX="6" RadiusY="6" Margin="0,0,12,0" />
                                <StackPanel Orientation="Vertical" Width="200">
                                    <TextBlock Text="{Binding Name}" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                    <TextBlock Text="{Binding Category}" FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}" />
                                </StackPanel>
                                <Button Style="{StaticResource DeleteButton}" Margin="8,0,0,0" Click="DeleteTask_Click" Tag="{Binding Id}">
                                    <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <StackPanel Grid.Row="3" Margin="0,16,0,0">
                    <Grid>
                        <TextBox x:Name="NewTaskNameTextBox" Margin="0,0,8,0" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" />
                        <TextBlock x:Name="PlaceholderText" Text="任务名称" Margin="4,3"
                                   Foreground="{DynamicResource TertiaryTextBrush}" IsHitTestVisible="False"
                                   Visibility="{Binding Text, ElementName=NewTaskNameTextBox, 
                                   Converter={StaticResource BooleanToVisibilityConverter}, 
                                   ConverterParameter=True}" />
                    </Grid>
                    <ComboBox x:Name="CategoryComboBox" Margin="0,8,0,0">
                        <ComboBoxItem Content="数学" />
                        <ComboBoxItem Content="英语" />
                        <ComboBoxItem Content="政治" />
                        <ComboBoxItem Content="专业课" />
                        <ComboBoxItem Content="其他" />
                    </ComboBox>
                </StackPanel>
                
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Style="{StaticResource SecondaryButton}" Click="CloseButton_Click" Margin="0,0,12,0">
                        取消
                    </Button>
                    <Button Style="{StaticResource PrimaryButton}" Click="AddTaskButton_Click">
                        添加任务
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>


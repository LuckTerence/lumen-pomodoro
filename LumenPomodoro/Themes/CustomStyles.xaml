<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <FontFamily x:Key="InterLight">pack://application:,,,/Fonts/#Inter Light</FontFamily>
    <FontFamily x:Key="InterRegular">pack://application:,,,/Fonts/#Inter</FontFamily>
    <FontFamily x:Key="InterSemiBold">pack://application:,,,/Fonts/#Inter SemiBold</FontFamily>

    <!-- Spotify 风格颜色系统 -->
    <Color x:Key="SpotifyCanvas">#121212</Color>
    <Color x:Key="SpotifySurface1">#181818</Color>
    <Color x:Key="SpotifySurface2">#282828</Color>
    <Color x:Key="SpotifySurface3">#3E3E3E</Color>
    <Color x:Key="SpotifyDivider">#2A2A2A</Color>

    <Color x:Key="SpotifyTextPrimary">#FFFFFF</Color>
    <Color x:Key="SpotifyTextSecondary">#B3B3B3</Color>
    <Color x:Key="SpotifyTextTertiary">#6A6A6A</Color>

    <Color x:Key="SpotifyGreen">#1DB954</Color>
    <Color x:Key="SpotifyGreenPressed">#169C46</Color>
    <Color x:Key="SpotifyLogoGreen">#1ED760</Color>
    <Color x:Key="SpotifyErrorRed">#F15E6C</Color>

    <!-- 语义色映射 -->
    <Color x:Key="SuccessColor">#1DB954</Color>
    <Color x:Key="FallbackTaskColor">#6A6A6A</Color>
    <Color x:Key="ArcBackgroundColor">#1A808080</Color>

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
    <SolidColorBrush x:Key="FallbackTaskBrush" Color="{StaticResource FallbackTaskColor}" />
    <SolidColorBrush x:Key="ArcBackgroundBrush" Color="{StaticResource ArcBackgroundColor}" />

    <!-- Spotify 风格画刷 -->
    <SolidColorBrush x:Key="SpotifyCanvasBrush" Color="{StaticResource SpotifyCanvas}" />
    <SolidColorBrush x:Key="SpotifySurface1Brush" Color="{StaticResource SpotifySurface1}" />
    <SolidColorBrush x:Key="SpotifySurface2Brush" Color="{StaticResource SpotifySurface2}" />
    <SolidColorBrush x:Key="SpotifySurface3Brush" Color="{StaticResource SpotifySurface3}" />
    <SolidColorBrush x:Key="SpotifyDividerBrush" Color="{StaticResource SpotifyDivider}" />
    <SolidColorBrush x:Key="SpotifyTextPrimaryBrush" Color="{StaticResource SpotifyTextPrimary}" />
    <SolidColorBrush x:Key="SpotifyTextSecondaryBrush" Color="{StaticResource SpotifyTextSecondary}" />
    <SolidColorBrush x:Key="SpotifyTextTertiaryBrush" Color="{StaticResource SpotifyTextTertiary}" />
    <SolidColorBrush x:Key="SpotifyGreenBrush" Color="{StaticResource SpotifyGreen}" />
    <SolidColorBrush x:Key="SpotifyGreenPressedBrush" Color="{StaticResource SpotifyGreenPressed}" />
    <SolidColorBrush x:Key="SpotifyErrorRedBrush" Color="{StaticResource SpotifyErrorRed}" />

    <Style x:Key="TimerText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="80" />
        <Setter Property="FontWeight" Value="Light" />
        <Setter Property="FontFamily" Value="{StaticResource InterLight}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StatNumber" TargetType="TextBlock">
        <Setter Property="FontSize" Value="40" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="21" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Margin" Value="0,0,0,10" />
    </Style>

    <Style x:Key="SettingLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="{StaticResource InterRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="BodyText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontFamily" Value="{StaticResource InterRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="Caption" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="{StaticResource InterRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="FinePrint" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="{StaticResource InterRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
    </Style>

    <Style x:Key="StateLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <!-- Spotify 风格按钮样式 -->
    <Style x:Key="SpotifyButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SpotifyGreenBrush}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="Padding" Value="24,12" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="24"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Spotify 胶囊按钮（带弹性动画） -->
    <Style x:Key="SpotifyPillButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SpotifyGreenBrush}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontFamily" Value="{StaticResource InterSemiBold}" />
        <Setter Property="Padding" Value="32,10" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="50"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 深色主题 ComboBox 样式 -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#3327272A" />
        <Setter Property="Foreground" Value="#B3FFFFFF" />
        <Setter Property="BorderBrush" Value="#26FFFFFF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <!-- 深色主题 ComboBoxItem 样式 -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#3327272A" />
        <Setter Property="Foreground" Value="#B3FFFFFF" />
        <Setter Property="Padding" Value="8,4" />
    </Style>

    <!-- Spotify 圆形播放按钮 -->
    <Style x:Key="SpotifyPlayButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SpotifyGreenBrush}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Width" Value="56" />
        <Setter Property="Height" Value="56" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Ellipse x:Name="circle"
                                 Fill="{TemplateBinding Background}"
                                 Width="{TemplateBinding Width}"
                                 Height="{TemplateBinding Height}"
                                 RenderTransformOrigin="0.5,0.5">
                            <Ellipse.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="circle" Property="Fill" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="circle" Property="Fill" Value="{DynamicResource SpotifyGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
# 2026-05-25

## PRD V2.0 重写 + 需求侧优化

### PRD 文档更新
- 重写了 `docs/PRD.md` 从 V1.1 → V2.0
- 补充了所有已实现功能的需求描述：走神检测、专注评分、笔记、洞察引擎、热力图、科目均衡分析、每日复盘、考试倒计时、数据导出、灵动岛等
- 新增第 3.3 节"反需求清单"，明确记录不做的事项及理由
- 更新了版本路线图，反映实际开发进度
- 更新了数据模型文档，补充 `quality_score`、`notes`、`schema_version` 等字段

### 代码优化

#### 1. 预设任务模板（StorageService.cs）
- 原有 4 个通用默认任务（"专注""学习""阅读""复习"）→ 改为 5 个考研科目任务
- 数学(#3B82F6)、英语(#10B981)、政治(#EF4444)、专业课(#8B5CF6)、复盘与整理(#6B7280)
- 每个任务正确设置了 Category 字段

#### 2. Category 字段修复（TasksViewModel.cs + TasksPage.xaml(.cs)）
- TasksViewModel 新增 `NewTaskCategory` 属性 + `AvailableCategories` 静态列表
- TasksPage.xaml 新增分类下拉选择器（数学/英语/政治/专业课/其他）
- AddTask 方法现在正确赋值 Category 字段

#### 3. 长休息自动提示（MainViewModel.cs + TimerPage.xaml）
- 新增 `SuggestLongBreak` 和 `TodayCompletedCount` 属性
- 专注完成后，若今日已完成番茄数 % LongBreakInterval == 0，自动高亮长休息建议
- TimerPage 长休息按钮在激活时显示金色高亮 + "建议长休息（已完成 N 个番茄）"文案
- StartFocus/ResetFocus 时重置建议状态

### 构建验证（最终）
- .NET 8 SDK 编译通过（0 错误）
- 59/62 测试通过，2 跳过，1 flaky（Dispose_StopsCamera 时序问题）

## P2 优化

### 4. 数据 Schema 版本化（Settings.cs + StorageService.cs）
- Settings 模型新增 `SchemaVersion` 字段（默认 1）
- StorageService 新增 `_schema.json` 元数据文件跟踪当前版本
- 新增 `RunMigrations()` → `MigrateV0ToV1()` 迁移管线
- 未来格式变更只需加新 migration 方法即可平滑升级

### 5. 功能开关系统（Settings + SettingsViewModel + SettingsPage + MainViewModel + StatsViewModel）
- Settings 新增 4 个开关：`InsightsEnabled`、`DailyReportEnabled`、`ExamCountdownEnabled`、`DynamicIslandEnabled`（默认全开）
- SettingsPage.xaml 新增"实验功能"区块，4 个 ToggleSwitch
- MainViewModel: ExamCountdown 受开关控制，DynamicIsland 启停受开关控制
- MainWindow: DailyReport 弹窗受开关控制
- StatsViewModel: 洞察/目标进度/对比/效率趋势受 InsightsEnabled 开关控制（关闭时返回空列表）

## 设计补充优化

### 6. README 同步 + LICENSE + 清理
- README 全面重写：.NET 9→.NET 8，功能列表从 6 项扩展到 15+ 项分组展示
- 创建 Apache 2.0 LICENSE 文件
- 删除放错位置的 DESIGN-swiftui.md（Spotify SwiftUI 设计指南，与 WPF 项目无关）
- 修复 SettingsPage 文案 bug："弹窗提醒"描述错误写成灵动岛

### 7. 专注中显示任务名（TimerPage.xaml + MainViewModel.cs）
- 专注中/暂停中状态下显示"正在专注 · 考研数学"
- 完成面板新增"刚刚完成：考研数学 · 25 分钟"摘要行

### 8. 今日目标微型进度条（TimerPage.xaml + MainViewModel.cs）
- 主界面底部新增极光渐变进度条，直观展示每日番茄目标完成度
- 新增 TodayPomodoroProgress / TodayPomodoroBarWidth 计算属性

## 方向三：统计页面行动化

### 9. 洞察卡片 — 可操作建议（InsightModels.cs + InsightEngine.cs + StatsPage.xaml）
- Insight 模型新增 `ActionHint` 字段
- InsightEngine 为峰值时段/最佳时段/趋势下降/任务不足/休息太久等洞察生成绿色行动建议（如"试试把最重要科目安排在这个时段"）
- StatsPage 每张卡片底部显示绿色斜体建议
- 新增 `NonEmptyToVisibleConverter` 转换器

### 10. 科目均衡 — "添加任务"按钮（StatsPage.xaml + MainWindow.xaml.cs）
- 零分钟科目警告旁显示 "+ 添加缺失科目的任务" 按钮
- 点击跳转到任务管理页面

### 11. 日报对话框 — "补上科目"建议（DailyReport + DailyReportDialog）
- DailyReport 新增 `CategorySuggestion` 字段
- GetYesterdayReport 自动分析昨日未学习的科目并生成建议
- 日报对话框中绿色显示"昨天没有学习「X」「Y」，今天可以补上进度"

### 构建验证
- 编译 0 错误，测试 59/62 通过（1 flaky）
